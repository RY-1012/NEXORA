# GitHub Actions CI (Optional)

This workflow runs tests on every push to main.

## Setup

1. Create `.github/workflows/ci.yml` file in root
2. Push to GitHub
3. Actions run automatically

## Usage

All tests must pass before merging. See workflow file for details.
